interface ILink{
  link: string;
  headerImage: string;
  sideBarImage: string;
  text: string;
}

interface IFiat{
  symbol: string;
  name: string;
}

interface ILang{
  symbol: string;
  name: string;
}